@{
    ViewBag.Title = "IndexHtmlAction";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

<h2> </h2>

@Html.Action("IndexListing", "Manufacturer")


@section ScriptsDelete{
    <script type="text/javascript">

        $(function () {
          //  $(".delManufacturer").click(function (e) {
            $('body').on('click', '.delManufacturer', function (e) {
                e.preventDefault()
                if (confirm('Do you want to delete this Manufacturer?')) {
                    $.ajax({
                        url: "@Url.Action("Delete", "Manufacturer")",
                        data: { id: $(this).attr('data-id') },
                        type: 'POST',
                        success: function (data) {
                            if (data == "OK")
                                window.location.href = "@Url.Action("IndexHtmlAction", "Manufacturer")"
                            else
                                alert("Error!!Good has this Manufacturer")
                        },
                        error: function (data) {
                            alert(data);
                        }
                    });
                }
            });
        });

    </script>
}

