@{
    ViewBag.Title = "IndexHtmlAction";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

<h2> </h2>

@Html.Action("IndexListing", "Order")


@section ScriptsDelete{
    <script type="text/javascript">

        $(function () {
           // $(".delOrder").click(function (e) {
            $('body').on('click', '.delOrder', function (e) {
                e.preventDefault()
                if (confirm('Do you want to delete this Order?')) {
                    $.ajax({
                        url: "@Url.Action("Delete", "Order")",
                        data: { id: $(this).attr('data-id') },
                        type: 'POST',
                        success: function (data) {
                            if (data == "OK")
                                window.location.href = "@Url.Action("IndexHtmlAction", "Order")"
                            else
                                alert("Error!!Good has this Order")
                        },
                        error: function (data) {
                            alert(data);
                        }
                    });
                }
            });
        });

    </script>
}


