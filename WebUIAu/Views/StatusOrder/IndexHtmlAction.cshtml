@{
    ViewBag.Title = "IndexHtmlAction";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

<h2> </h2>

@Html.Action("IndexListing", "StatusOrder")

@section ScriptsDelete{
    <script type="text/javascript">

        $(function () {
         //   $(".delStatus").click(function (e) {
            $('body').on('click', '.delStatus', function (e) {
                e.preventDefault()
                if (confirm('Do you want to delete this Status?')) {
                    $.ajax({
                        url: "@Url.Action("Delete", "Status")",
                        data: { id: $(this).attr('data-id') },
                        type: 'POST',
                        success: function (data) {
                            if (data == "OK")
                                window.location.href = "@Url.Action("IndexHtmlAction", "Status")"
                            else
                                alert("Error!! Product has this Status")
                        },
                        error: function (data) {
                            alert(data);
                        }
                    });
                }
            });
        });

    </script>
}